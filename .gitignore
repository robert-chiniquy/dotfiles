*.pyc
.venv
.vagrant
.vim
*.log
.DS_Store

# Secrets and credentials - never commit
.secrets
.env
.env.*
*.pem
*.key
id_rsa*
*.p12
*.pfx
.aws/credentials
.netrc
.claude/mcp.json
.claude/tasks/

*/target/release/*

*/target/*


# Compiled binaries
bin/fclones
bin/vaporwave-overlay
bin/wallpapper
metal-overlay/vaporwave-overlay
metal-overlay/VaporwaveOverlay.app/Contents/MacOS/vaporwave-overlay
bin/gistcopy
bin/squire
.claude/backups/
.claude/sessions/
.claude/settings.json.bak
.claude/mcp-needs-auth-cache.json
.Codex/
LEARNINGS.md

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Locally-managed Claude state (per-machine, not for git)
.claude/.mcp.json
.claude/.last-cleanup
.claude/.last-update-result.json
.claude/remote-settings.json

# Codebase memory and vit local indexes/state
.codebase-memory/
.vit/

# Local squire scratch + metrics data
scripts/.claude/
scripts/squire-metrics.jsonl

# Personal safety backups (file.bak.pre-<change>)
*.bak.pre-*

# Compiled tool binaries (installed via brew/go install, not for git)
bin/golangci-lint
bin/gotestsum

# Demo / scratch docs
DEMO_CODEX_LIMITED.md

# Symlink to sqfan project's local skill source — dev-machine convenience
.claude/skills/sqfan
